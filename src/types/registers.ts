import { Register } from "../classes/register";

export type Registers = {
  IR: Register;
  MAR: Register;
  MDR: Register;
  PC: Register;
  MBR: Register;
  SP: Register;
  LV: Register;
  CPP: Register;
  TOS: Register;
  OPC: Register;
  H: Register;
};
